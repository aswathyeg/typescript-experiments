import { velocity } from "./lib/velocity"

import {acceleration} from './lib/calculateAcceleration';

    const finalVelocity=new velocity();
    console.log(`The acceleration is ${acceleration(finalVelocity)}`)
